version: 0.2

phases:
  install:
    runtime-versions:
      docker: 19
  pre_build:
    commands:
      - docker -v
      - docker-compose -v
      - systemctl status docker
      - groups
      - docker-compose --verbose up -d
  build:
    commands:
      - echo "test build"

